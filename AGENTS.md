# DataAgent 通用开发原则

本项目目标是构建能够根据 TaskSpec、工具能力和运行反馈自主规划 Pipeline 的通用 Agent，不是为当前演示案例编写规则系统。

## 禁止事项

除非属于安全、权限、Schema 或确定性执行约束，否则禁止：

1. 根据用户原句、关键词或具体类别增加 if/else。
2. 在编排代码中出现“猫狗”“黑色衣服”“自拍”等任务实例名称。
3. 为单个测试案例写固定 Pipeline。
4. 通过固定澄清问题解决字段缺失。
5. 在检索模块中硬编码“需求关键词 → 算子名称”映射。
6. 因当前案例失败而直接增加默认值或静默兜底。
7. 未定位根因前修改代码。

## 每次修复必须先完成

1. 稳定复现问题。
2. 追踪完整数据流：
3. 明确问题属于哪一层：
   - 需求解析
   - Schema/TaskSpec
   - 算子检索
   - Pipeline规划
   - Plan校验
   - 执行
   - 结果评估
4. 写出单一根因假设及证据。
5. 提出通用修复方案，不得只覆盖当前示例。
6. 说明为什么不应使用关键词或任务特例修复。
7. 我确认设计后才能修改代码。

## 实现要求

每次通用机制修改必须至少提供：

1. 当前失败案例。
2. 两个结构相同、语义不同的泛化案例。
3. 一个反例或边界案例。
4. 修改前失败、修改后通过的自动化测试。
5. 完整测试结果。
6. 修改文件及职责说明。

## 泛化判断

把当前任务中的实体、属性、类别和路径全部替换后：

- 是否仍然无需修改代码？
- 是否只需要更换 TaskSpec 或算子元数据？
- Planner 是否能根据能力描述重新生成 Plan？
- 执行失败后是否能根据 Observation 重新规划？

若答案是否定的，则当前实现仍属于任务硬编码。