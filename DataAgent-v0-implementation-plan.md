# DataAgent 图片数据生产平台 v0 实施方案

## 1. 建设目标

DataAgent 是面向内部真实生产的图片数据处理 Agent 平台。需求提出者通过自然语言描述数据目标，平台将需求转化为可验证的结构化规格，生成并试跑三类候选 Pipeline，展示实际处理效果，经需求提出者确认后执行全量任务，并把有效 Pipeline 沉淀为可复用资产。

v0 的核心成功标准是：Pipeline 处理结果能够被需求提出者验证为满足要求，并且整个过程可追溯、可恢复、可回归、可复用。

首期范围：

- 只处理图片数据。
- 支持指定本地目录和从 Milvus 智能检索图片，也支持两者组合。
- 单次任务通常处理数千张图片。
- Windows 单机部署，支持 2～3 个任务并发。
- 视觉理解和规划能力通过阿里百炼兼容接口调用。
- 原始图片只读，筛选结果保存清单，裁剪、修复等产物写入独立版本目录。
- v0 不做互联网算子发现，不依赖 Data-Juicer；允许按统一协议封装其可复用算子。
- 生成式图片编辑仅预留模型接口，配置图片编辑模型后作为算子接入。

## 2. 用户闭环

1. 用户登录 DataAgent，创建图片任务并输入自然语言需求。
2. 用户选择本地目录、Milvus Collection 或两者组合。
3. 规划模型把需求转换为 `TaskSpec`，明确硬约束、语义约束、输出要求、验收方法及待确认歧义。
4. 用户确认 `TaskSpec` 后，系统优先检索相似历史 Pipeline，再使用已有算子组合候选方案。
5. 系统固定生成三类候选 Pipeline：
   - 保留优先：降低误删和过度处理风险。
   - 均衡方案：综合质量、保留量、成本和速度，作为默认推荐。
   - 质量优先：执行更严格的筛选、检测或加工。
6. 三条 Pipeline 使用同一批 100～300 张代表性图片试跑。
7. 平台展示处理前后图片、保留率、规则通过率、模型评价、失败样例、置信度、耗时和预计 API 成本。
8. 系统选择约 20～50 张边界样本，用户只需判断“合格/不合格”，不要求修改 Pipeline 或标签。
9. 用户选择一条 Pipeline 并批准全量执行。
10. 全量结果保存为不可变 `DatasetVersion`，Pipeline 和验收样本进入用户个人资产库。
11. 后续相似需求优先复用历史 Pipeline，但仍需在当前任务样本上回归试跑。

第一次提出新需求时没有验证集，平台只能报告规则检查、模型判断和多评估器一致性等代理指标，不能宣称真实准确率。用户审核过的边界样本逐步形成该类需求的最小验收集。

## 3. 总体架构

采用模块化单体控制面和独立执行 Worker，生产形态运行于 Windows Docker Desktop/WSL2。

### 3.1 Web 工作台

- React、TypeScript、Ant Design。
- 提供登录、需求创建、TaskSpec 确认、候选对比、样本审核、运行监控、结果查看和 Pipeline 库。
- 首屏直接进入任务工作台，不建设营销式首页。
- 使用紧凑、可扫描的内部生产工具布局。

### 3.2 API 与控制面

- FastAPI、Pydantic、SQLAlchemy。
- PostgreSQL 保存用户、任务、Pipeline、评测、数据版本和审计记录。
- 确定性状态机是任务状态的唯一事实来源。
- Agent 不能跳过状态校验、用户确认和安全检查直接修改任务状态。

### 3.3 Agent Runtime

- 使用 LangGraph 编排需求规划、候选生成、失败分析和修复决策。
- Agent 只输出结构化计划，不直接读写生产数据。
- 所有 Agent 输出必须通过 Pydantic Schema 校验。
- 规划模型和视觉模型通过统一 `ModelGateway` 接入。

### 3.4 任务执行

- Celery + Redis 管理异步任务和并发。
- 默认每 100 张图片一个分片。
- 支持 2～3 个并发任务、断点续跑、取消和失败重试。
- 内置算子由受控 Worker 执行；大模型生成的算子必须在 Docker 沙箱中执行。

### 3.5 文件与版本

- 原图目录始终只读，不允许原地修改或删除。
- 数据筛选结果保存不可变 Manifest。
- 裁剪、缩放、修复和格式转换等新图片写入独立版本目录。
- 每个文件记录来源 URI、哈希、处理节点、算子版本和输出 URI。

### 3.6 外部适配器

- `MilvusAdapter`：Collection 探测、字段映射、文搜图、标量过滤和结果去重。
- `ModelGateway`：兼容百炼 OpenAI 风格接口，统一管理规划模型、视觉模型、主备切换、限流、重试、成本和审计。
- `SandboxRunner`：控制容器权限、输入输出挂载、网络、资源和超时。
- 后续云端 GPU 通过新增 `GpuWorker` 和队列接入，不改变上层接口。

## 4. 核心数据对象

### 4.1 TaskSpec

包含：

- 用户原始需求与标准化目标。
- 本地目录、Milvus 或混合数据源。
- 输出形式：筛选、打标、去重、裁剪、缩放、修复、增强或组合。
- 硬约束：尺寸、格式、宽高比、数量和文件完整性等。
- 语义约束：图片内容、场景、主体和排除条件等。
- 质量偏好、成本限制和执行优先级。
- 验收方式、边界样本数和待确认歧义。
- owner、版本、创建时间和确认时间。

### 4.2 OperatorSpec

包含算子名称、版本、输入输出 Schema、参数 Schema、实现类型、依赖、资源限制、适用条件、测试结果和代码哈希。

### 4.3 PipelineVersion

包含算子有向无环图、参数、策略类别、Task 指纹、父版本、创建来源、试跑指标和发布状态。

### 4.4 TrialReport

包含试跑样本构成、处理前后结果、硬规则通过率、模型代理评价、评估器一致性、边界样本、保留率、异常率、耗时、成本和失败样例。

### 4.5 ReviewSet

保存需求提出者对边界样本的合格/不合格判断、所属 TaskSpec、Pipeline 版本和审核时间。

### 4.6 DatasetVersion

保存源文件清单、产物清单、文件哈希、Pipeline 版本、算子版本、模型版本、运行记录、质量报告和用户确认记录。

### 4.7 Run

状态流转：

```text
DRAFT
  -> PLANNING
  -> WAITING_SPEC_CONFIRMATION
  -> TRIAL_RUNNING
  -> WAITING_REVIEW
  -> FULL_RUNNING
  -> EVALUATING
  -> COMPLETED
```

任意可执行阶段可进入 `FAILED`、`CANCELLED` 或 `RETRYING`，但必须通过控制面定义的合法状态转换。

## 5. 主要 API

- `POST /auth/setup`：初始化首个本地账号。
- `POST /auth/login`、`POST /auth/logout`：登录和退出。
- `POST /tasks`：创建自然语言图片任务。
- `GET /tasks`、`GET /tasks/{id}`：查询用户自己的任务。
- `POST /tasks/{id}/confirm-spec`：确认结构化需求。
- `POST /tasks/{id}/generate-candidates`：生成并试跑三条候选 Pipeline。
- `GET /tasks/{id}/trial-report`：获得候选效果对比和边界样本。
- `POST /tasks/{id}/reviews`：提交样本合格/不合格判断。
- `POST /tasks/{id}/select-pipeline`：选择候选并批准全量执行。
- `GET /runs/{id}`、`POST /runs/{id}/cancel`：监控和取消运行。
- `GET /datasets/{id}`：查看结果版本和质量报告。
- `GET /pipelines`：检索个人历史 Pipeline。
- `POST /pipelines/{id}/run`：在新数据上回归并复用 Pipeline。
- `POST /data-sources/milvus/discover-schema`：探测 Milvus Collection 和字段。
- `POST /data-sources/milvus/validate-mapping`：验证记录到本地图片的映射。

本地账号密码使用 Argon2id 哈希，浏览器使用 HttpOnly Session Cookie。所有任务、Pipeline、数据版本和模型调用按 owner 隔离并记录审计日志。

## 6. Pipeline 与算子体系

### 6.1 首批内置算子

- 文件扫描、解码检查和损坏图片检测。
- 格式转换、尺寸、宽高比和方向检测。
- 模糊度、亮度、噪声、压缩损伤和基础质量统计。
- 精确哈希、感知哈希和向量相似去重。
- 裁剪、缩放、旋转、颜色调整、增强和格式输出。
- OCR、视觉分类、语义打标和视觉模型需求判定。
- Milvus 检索、随机采样、分层采样和聚类采样。
- Manifest 生成、数据版本冻结和质量报告。

### 6.2 候选生成优先级

1. 检索相似历史 Pipeline，并在当前试跑集回归。
2. 使用已注册算子重新组合。
3. 缺少能力时，让规划模型生成基于平台 Operator SDK 的 Python 算子。
4. v0 不从互联网自动下载算子或依赖。

### 6.3 三类候选

- 保留优先：阈值更宽松，保留低置信度样本，减少误删。
- 均衡方案：按质量、保留率、成本和速度综合评分。
- 质量优先：阈值更严格，必要时增加视觉模型复核。

三个候选必须使用相同样本、相同模型版本和统一资源口径进行比较。

## 7. 效果评估

候选排序依次考虑：

1. 硬约束是否 100% 通过。
2. 用户审核边界样本的通过率。
3. 两个视觉评估器的一致性和置信度。
4. 数据保留率及异常删除率。
5. 输出图片完整性和质量变化。
6. 运行耗时、API 成本和失败率。

平台界面必须展示真实试跑产物和失败样例，不能只展示 Agent 对 Pipeline 的文字解释。

用户尚未审核任何样本时，指标必须标记为“代理评估”；有人工审核后才能展示基于当前验收集计算的任务级通过率。

## 8. 生成算子安全与自迭代

### 8.1 沙箱规则

- 使用 Docker Desktop/WSL2 Linux 容器。
- 非 root 用户运行。
- 原始图片只读挂载，输出使用独立目录。
- 默认禁止外网，不向容器暴露模型密钥。
- 限制 CPU、内存、进程数和执行时间。
- 禁止访问 Docker Socket 和宿主其他目录。
- 依赖来自平台维护的预构建基础镜像，不允许运行时任意安装包。
- 模型调用由平台内置模型算子通过 `ModelGateway` 完成。

### 8.2 准入流程

生成算子依次经过：代码静态检查、Schema 校验、内置单元样例、容器小样本试跑、输出文件检查、异常率检查和历史验收样本回归。全部通过后才能成为当前 Pipeline 的候选版本。

### 8.3 自动修复

- 参数、依赖或非语义代码错误：修复后回归通过即可生成新版本并自动续跑。
- 删除比例突变、空结果、输出损坏或模型结果漂移：停止当前分片并重新进入试跑。
- 修改 TaskSpec、筛选语义或 Pipeline 结构：必须重新由需求提出者确认。
- 每次运行最多自动修复三轮；仍失败则保留诊断信息并停止，不无限循环。
- 自动修复只能生成新版本，不能覆盖已确认的算子、Pipeline 或 DatasetVersion。

## 9. Milvus 接入

现有 Milvus 已支持文搜图。平台首次接入时自动探测 Collection Schema：

- 如果记录包含本地路径或 URI，配置字段映射后直接读取。
- 如果只有图片 ID，支持导入 ID 到本地路径的 CSV 或数据库映射。
- 如果部分本地图片尚未建立索引，v0 允许目录直接处理；增量向量化作为可插拔能力实现。
- 无法从检索记录定位原始图片时，只允许查看检索统计，禁止启动处理任务。

Milvus 连接凭据与模型密钥只从环境变量或本地 Secret 文件加载，不写入数据库、日志或前端。

## 10. 部署与配置

生产部署使用 Docker Compose，至少包含：

- `web`
- `api`
- `worker`
- `postgres`
- `redis`
- `sandbox-runner`

现有 Milvus 作为外部服务连接，不重复部署。

当前开发机器尚未安装 Docker，因此开发阶段提供 SQLite 和本地后台任务回退，用于跑通需求、试跑、审核、全量执行和数据版本流程。大模型生成代码在 Docker 沙箱可用前只允许生成和静态检查，禁止实际执行。

模型配置从本地 Secret 文件读取。当前配置包含：

- 一个规划模型。
- 一个视觉模型。
- 一套百炼 API Key 和 Base URL。

代码库只提供 `.env.example`，真实 Secret 文件必须加入 `.gitignore`，任何接口、日志和错误信息都不得输出完整 API Key。

## 11. 测试方案

### 11.1 契约和单元测试

- TaskSpec、OperatorSpec、PipelineVersion 和 Agent 输出 Schema 校验。
- 状态机合法与非法转换。
- 路径规范化、目录越界和原图只读检查。
- 图片解码、尺寸、模糊度、亮度、哈希和去重算子。
- 三类策略的阈值差异和候选评分。

### 11.2 集成测试

- Milvus Schema 探测、字段映射、文搜图和失效路径。
- 百炼超时、限流、无效响应、主备模型切换和成本统计。
- Docker 网络隔离、资源限制、只读挂载和超时终止。
- PostgreSQL、Redis 和 Worker 重启后的任务恢复。
- 算子崩溃、空输出、损坏图片、异常删除率和自动修复。

### 11.3 端到端测试

- 首个用户初始化和登录。
- 自然语言需求生成并确认 TaskSpec。
- 在同一批图片上生成三条候选及效果报告。
- 用户审核边界样本并选择 Pipeline。
- 数千张图片分片执行，生成不可变 DatasetVersion。
- 使用历史 Pipeline 创建新任务并执行回归试跑。
- 任务取消、断点续跑、失败恢复和版本回滚。
- 两到三个并发任务的资源隔离和队列调度。

## 12. v0 上线验收标准

- 原图在任何成功或失败场景下都不会被修改。
- 每个正式结果都能追溯到数据源、TaskSpec、Pipeline、算子、模型、参数、运行记录和用户确认。
- 每个新需求至少生成三类候选，并展示可核验的试跑效果。
- 硬约束默认要求 100% 通过。
- 语义质量由视觉模型代理指标和用户抽样验收共同确认。
- 用户确认的 Pipeline 能被检索、回归试跑和版本化复用。
- 数千张图片任务支持暂停、恢复、取消和回滚。
- 单机支持 2～3 个任务并发，且任务间数据与资源隔离。
- 非语义性失败可在最多三轮内自动修复并续跑；语义变更必须重新确认。
- 规划模型、视觉模型、Milvus、文件存储和未来 GPU Worker 均通过稳定适配器接入。

## 13. 实施顺序

1. 建立工程骨架、配置管理、账号认证、数据库和状态机。
2. 跑通本地目录扫描、基础图片算子、Manifest 和不可变版本输出。
3. 接入规划模型，生成并确认结构化 TaskSpec。
4. 实现三类 Pipeline、统一试跑样本和 TrialReport。
5. 完成候选对比、边界样本审核和全量任务工作台。
6. 接入视觉模型代理评估和双评估器一致性报告。
7. 接入 Milvus Schema 探测、路径映射和文搜图。
8. 完成个人 Pipeline 库、任务指纹、回归试跑和版本复用。
9. 接入 Docker 沙箱、生成算子静态检查、试跑和自动修复。
10. 完成并发、恢复、安全、端到端测试及生产部署配置。
